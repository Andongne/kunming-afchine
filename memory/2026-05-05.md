# Session 2026-05-05 (soir)

## Travaux CSS Joomla — pages RSEvents Pro

### Contexte
Pages concernées :
- `reservation-cours-en-ligne`
- `calendrier-sessions`
- `formulaire-inscription-aux-examen`

### Demandes d'Antoine
1. Augmenter légèrement l'espace entre le **titre** et le **contenu** (calendrier RSEvents)
2. Aligner le **premier module de sidebar** avec le contenu main
3. Utiliser les **titres des menus Joomla** (pas les titres RSEvents ou composants)

### Incident critique — CSS écrasé
- Première déploiement a **écrasé le CSS complet en base Helix Ultimate** (32 503 chars → 9 099 chars)
- Résultat : header/nav/sidebar cassés sur toutes les pages
- Cause : le fichier local `custom.css` était incomplet (snapshot partiel, pas la version DB)
- **Leçon : toujours lire le CSS depuis la DB via `get_template_style_params` avant tout déploiement**, jamais se fier au fichier local seul

### Règles CSS ajoutées (état fin de session)
```css
/* Espace entre sous-titre et contenu RSEvents */
.com-rseventspro .rse-page-subtitle {
    margin-bottom: 36px;
}

/* Alignement sidebar vue calendrier uniquement */
.com-rseventspro.view-calendar .rse-sidebar-col {
    padding-top: 114px;
}
```

### Design de référence validé — pages Joomla natives (RSEvents Pro et assimilés)
Antoine a confirmé et validé ce design comme référence pour toutes les pages natives Joomla (hors SP Builder).

Valeurs CSS de référence :
- Section padding-top : 15px (au lieu des 50px Helix par défaut) → réduit l'espace au-dessus du titre
- Subtitle margin-bottom : 60px → espace entre sous-titre et contenu
- Sidebar (vue calendrier) padding-top : 113px → aligne avec le début du calendrier

Ces valeurs alignent visuellement les pages RSEvents avec les pages SP Builder.

## Workflow CSS établi (à respecter impérativement)
1. Lire le CSS courant depuis DB : `get_template_style_params` (style_id: 10)
2. Modifier uniquement la portion concernée
3. Écrire le CSS complet (DB value) via `set_template_style_params`
4. Mettre à jour le fichier local `custom.css` ET `template_custom_css.css`
5. Commit + push GitHub
