# 2026-05-09 — Session dev.kunming-afchine.org

## État au fil de la session

### Migration prod → dev : TERMINÉE ✓
- DB : 187 tables migrées (`bwhwo_*` → `dev_*`), datetime issues résolus
- Fichiers template : 325 fichiers synchronisés
- Images : sync complet (2025/2026 + partenaires + SP Builder placeholders)
- configuration.php dev : `live_site`, `dbprefix`, `debug=false`, `caching=0` corrects

### Corrections appliquées sur dev
- **Erreur MySQL 500** (`mysqli object is already closed`) : corrigé dans `MysqliDriver.php` via try/catch sur `close()`
- **Plugin debug Joomla** : désactivé (`dev_extensions`)
- **Images manquantes** : `images/partenaires/` (23 fichiers) + `media/com_sppagebuilder/placeholder/` (792 fichiers) + `placeholder.svg` copiés depuis prod
- **URLs hardcodées prod** dans `dev_content` : remplacées par URLs relatives

### État dev à 10h57
- Site fonctionnel, pas d'erreur 500
- Couleur de fond : correcte
- Images : correctes
- Prêt pour les prochaines étapes (Community Builder, refonte homepage)

## Optimisations perf/accessibilité déployées (prod + dev)
- `defer` sur mobile-menu.js, calendar-i18n.js (template index.php)
- `preload` image hero homepage
- `aria-label` bouton close menu mobile
- Plugin `afk_defer` : `defer` sur 7 scripts SP Builder
- `MysqliDriver.php` : fix try/catch (PHP 8.x + Joomla 4)
- `afk-styles.css` : contrastes WCAG AA (#888 → #767676/#5e5e5e)
- Résultat PageSpeed dev : accessibilité mobile 91→96, bureau 87→91, perf bureau 89→93

## Prochaines étapes planifiées
1. **Backend utilisateur (lundi ou jeudi)** — Community Builder sur dev
   - Profil utilisateur (avatar, ville, niveau de français)
   - Historique inscriptions via RSEvents Pro
   - Création automatique de compte depuis formulaires d'inscription
   - RGPD : consentement + vérification email
2. Ne pas mettre à jour PHP vers 8.4 avant RSEvents Pro 1.15+

## Notes techniques
- MySQL user prod/dev : `root` / `Igouzing831917`
- DB name : `Kunming.org` (partagée prod et dev)
- falang-inject endpoint : `https://kunming-afchine.org/falang-inject/sppb5.php`
- Template : `shaper_languageschool`
- Dev URL : `https://dev.kunming-afchine.org`
