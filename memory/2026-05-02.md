# Session 2026-05-02

## Travaux du matin — CSS mobile homepage

### Résumé
- Cards homepage refonte class-based (résistante aux réorganisations SP Builder)
- Bouton "En savoir +" Blog TCF Canada — couleur rouge corrigée (règle `#sppb-addon-372ee672 a:not(.sppb-btn)`)
- template.css = généré dynamiquement par Helix Ultimate, ne pas modifier via SFTP
- Browser tool OpenClaw réparé (playwright-core + stale lock + --disable-gpu)

## Travaux de l'après-midi — dev.kunming-afchine.org

### État : EN COURS (reprendre le 3 mai)

### Accompli
- ✅ DNS créé : `dev A 155.133.142.2` + `dev AAAA 2001:4b98:dc6:253::2`
- ✅ Token API Gandi : `Openclaw-afkunming`, Bearer auth (pas Apikey), clé `20f251e8284f557908f2c0a46bbc200d352cff07`
- ✅ 188/188 tables DB clonées `bwhwo_` → `dev_` (SQL strict mode désactivé pour `0000-00-00`)
- ✅ Fichiers Joomla copiés (prod→dev) via script PHP serveur-à-serveur
- ✅ `robots.txt` → Disallow all + `X-Robots-Tag: noindex` dans .htaccess
- ✅ Site accessible et Joomla démarre

### Problèmes restants (à corriger le 3 mai)
1. **Images manquantes** sur les cards homepage — dossier `images/` probablement incomplet
2. **Erreur `mysqli object is already closed`** en bas de page (debug activé)
3. **Autres problèmes non détaillés** — Antoine a stoppé pour ce soir

### Configuration dev actuelle
- Fichier : `vhosts/dev.kunming-afchine.org/htdocs/configuration.php` (généré depuis prod)
- DB prefix : `dev_`
- `$debug = true`, `$caching = 0`, `$gzip = false`, `$mailonline = false`
- `$error_reporting = 'maximum'`
- `$offset = 'Asia/Shanghai'`
- `$live_site = 'https://dev.kunming-afchine.org'`

### Fichiers temporaires à nettoyer
- `/tmp/cfg_prod.php`, `/tmp/cfg_dev_final.php` — configs PHP locales
- `/tmp/.htaccess_prod`, `/tmp/.htaccess_dev` — htaccess
- `/tmp/dev_clone_db.php`, `/tmp/dev_copy_files.php` — scripts de déploiement (déjà supprimés du serveur)

### Notes techniques
- Gandi SFTP : pas de shell SSH → copie de fichiers via script PHP uploadé sur prod puis exécuté
- L'erreur `error_reporting('development')` → Joomla 4 ne supporte pas cette valeur dans framework.php → utiliser `'maximum'` ou `'default'`
- La copie d'`administrator/components` a été faite composant par composant (timeout sur le dossier complet)
- `index.html` (page Gandi par défaut) doit être supprimé pour laisser passer Joomla

## Gandi API
- Token : `GANDI_API_TOKEN=20f251e8284f557908f2c0a46bbc200d352cff07`
- Auth : `Authorization: Bearer <token>` (pas Apikey)
- Endpoints actifs : LiveDNS (`/v5/livedns/...`)
- SimpleHosting API retourne `[]` — LAMP sd6 = ancien produit non gérable via API

## Browser tool OpenClaw
- Fonctionnel avec playwright-core@1.59.1 + `--disable-gpu` dans extraArgs
- En cas de blocage : `rm -rf /data/.openclaw/browser/openclaw/user-data && pkill -9 -f chromium`
