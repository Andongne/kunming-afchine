## Session 2026-04-30 (06:56 — fin) — Mobile AFK

---

## Session matin (suite) — Footer & modules mobile homepage + page certifications

### Corrections footer mobile Contact
- Ligne LinkedIn supprimée
- Titre "CONTACT" → h2, blanc, 1.4rem, gras, séparateur supprimé
- QR code centré garanti via CSS
- Copyright footer (#sp-footer2) masqué sur mobile
- Module 258 : contenu self-contained, assignment=0

### "Notre actualité" — résolu
- Bordure rouge pointillée supprimée via JS (override inline SP Builder)
- Fond blanc + filet rose rgba(218,0,46,0.2) appliqué via Helix custom_css (cohérent avec "On parle de nous")
- Liens en bleu nuit, dates en gris, bouton respecte sa couleur via :not(.sppb-btn)
- Addon porteur : c750c6f6 (container), 372ee672 (module)

### Page certifications mobile (page-40)
- Cause : SP Builder masquait sections/colonnes sur mobile (display:none via JS)
- Fix CSS : `.sp-page-builder.page-40 .page-content .sppb-section { display:block !important; ... }`
- Fix JS dans Helix custom_js : force visibilité de tous les éléments SP Builder sur page-40 mobile, SAUF éléments avec classes sppb-hidden-xs / sppb-hidden-sm (sidebar et CTA respectés)
- Script respecte les paramètres de visibilité responsive SP Builder

### Fichiers modifiés / DB
- `mobile_cards.css` (v=20260430g)
- Helix template_styles id=10 : custom_css + custom_js
- Snapshots commités dans `templates/shaper_languageschool/css/template_custom_css.css` et `custom_css.css`
- Module 258 "Contact mobile" via Joomla API

### Fix SP Builder mobile — étendu (2026-05-01)
- `fixCertPage` rendu générique : cible `#sp-page-builder` (toutes pages) au lieu de `#sp-page-builder.page-40`
- Déclenché par la page `/en/certifications-and-diplomas/advice-and-preparation/video-tcf-canada` (page-288) invisible sur mobile
- Même racine que page-40 : éléments SP Builder non affichés sur mobile

### Menu mobile — résolu
- Cause : handler jQuery du template ne déclenchait pas sur mobile (bug non identifié, probablement lié au no-conflict jQuery)
- Fix propre : fichier dédié `templates/shaper_languageschool/js/mobile-menu.js`
  - Ouverture : click natif sur `#offcanvas-toggler` → `classList.add('offcanvas-active')`
  - Fermeture : `.close-offcanvas` + `.offcanvas-overlay`
  - Sous-menus : délégation sur `.offcanvas-inner` pour les `.menu-toggler` (boutons +)
  - Toggle : `menu-parent-open` class + `child.style.display`
- Chargé via `<script>` dans `index.php`
- Module 258 nettoyé (bandaid retiré)

### Points techniques clés
- Helix custom_css INLINE dans <head> → appliqué après mobile_cards.css mais avant SP Builder inline CSS
- Spécificité CSS : ID (#sppb-addon-XXX) = (1,0,0) > classe (.sppb-section) = (0,1,0)
- !important écrase spécificité mais quand deux !important s'affrontent, la spécificité reprend ses droits
- La cause réelle du masquage sur page-40 reste non identifiée (probablement lié à x-data="easystoreProductList" ou au JS SP Builder d'animation)
